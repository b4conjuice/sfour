export const newNoteUrl = 'https://s4.dlopez.app/notes/new'

export const editNoteUrl = (noteId: string) =>
  `https://s4.dlopez.app/notes/${noteId}`

export const markdownNoteUrl = (noteId: string) =>
  `https://md.n4.dlopez.app/${noteId}`
